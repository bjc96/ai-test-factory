// 内置示例需求文档，方便演示
module.exports = {
  'ERP采购管理模块': `ERP企业资源管理系统 — 采购管理模块需求

一、采购订单管理

1. 新建采购订单
   - 用户选择供应商（从供应商档案中选择），选择采购员，添加采购明细行
   - 每行明细包含：物料（从物料档案中选择）、数量（正整数）、单价（正数，保留两位小数）
   - 系统自动计算每行小计（数量×单价）和订单总金额
   - 支持添加多行和删除行
   - 提交后采购单状态变为「待审批」，同时系统自动生成一条应付账款记录（到期日为30天后）
   - 验证规则：
     * 供应商为必选，否则提示"请选择供应商"
     * 至少添加一条采购明细，否则提示"请至少添加一条明细"
     * 数量必须大于0
     * 单价必须大于0
   - 物料列表仅显示非成品类物料

2. 采购订单列表
   - 以表格形式展示所有采购订单，列包含：单号、供应商名称、金额、状态、创建日期、操作按钮
   - 新订单排在列表最前面
   - 空数据时显示"暂无采购订单，点击右上角新建"
   - 状态包含：草稿、待审批、已通过、已入库、已关闭

3. 采购订单审批
   - 「待审批」状态的订单显示「审批」按钮
   - 点击审批后，订单状态变更为「已通过」，记录审批时间
   - 审批通过后的订单显示「收货」按钮

4. 采购入库（收货）
   - 点击「收货」按钮弹出收货对话框
   - 显示订单中每项物料的名称、订购数量、收货数量输入框、收货仓库下拉框
   - 收货后系统自动增加对应仓库的库存数量
   - 同时生成入库记录写入出入库流水
   - 全部收货后订单状态变更为「已入库」

5. 采购订单详情查看
   - 点击「详情」按钮弹出详情弹窗
   - 显示供应商名称、总金额、状态、创建时间
   - 以表格显示所有明细：物料名称、数量、单价、小计

二、应付账款管理（随采购自动生成）

1. 提交采购订单时自动生成应付账款记录
2. 在财务管理模块中展示应付账款列表，支持付款操作`,

  '登录模块': `登录模块功能需求：

1. 登录功能：
   - 用户输入正确的用户名和密码，点击登录按钮，成功登录并跳转首页
   - 用户输入错误的用户名或密码，点击登录按钮，提示"用户名或密码错误"
   - 用户不输入用户名和密码，点击登录按钮，提示"请输入用户名和密码"
   - 连续5次输入错误密码，账号锁定30分钟，提示"账号已被锁定"
   - 支持 Enter 键快捷登录

2. 图形验证码：
   - 登录页面显示图形验证码
   - 验证码输入错误提示"验证码错误，请重新输入"
   - 点击验证码图片可刷新验证码
   - 验证码不区分大小写

3. 注册功能：
   - 用户点击注册，填写用户名、密码、确认密码、手机号
   - 用户名长度 4-20 位，仅支持字母数字和下划线
   - 密码长度 6-20 位，必须包含字母和数字
   - 用户名已存在时提示"用户名已被注册"
   - 两次密码输入不一致时提示"两次密码不一致"
   - 手机号格式不正确时提示"请输入正确的手机号"

4. 记住密码：
   - 用户勾选"记住密码"，下次打开页面自动填充用户名和密码
   - 密码使用 AES 加密存储在本地

5. 忘记密码：
   - 点击忘记密码，通过手机验证码重置密码
   - 验证码有效期 5 分钟
   - 新密码不能与旧密码相同`,

  '文件上传模块': `文件上传功能需求：

1. 文件上传：
   - 支持上传单个文件，文件大小不超过 100MB
   - 支持批量上传，一次最多 10 个文件
   - 上传时显示进度条（百分比 + 速度）
   - 上传完成后显示文件列表
   - 文件名为空时提示"请选择文件"
   - 文件超过 100MB 时提示"文件大小不能超过100MB"
   - 上传数量超过 10 个时提示"一次最多上传10个文件"

2. 文件格式限制：
   - 允许上传：jpg, png, gif, pdf, doc, docx, xls, xlsx, ppt, pptx, zip, rar, 7z
   - 上传不支持的文件格式时提示"不支持该文件格式"
   - 上传空文件（0字节）时提示"不能上传空文件"
   - 文件名长度不超过 255 个字符

3. 断点续传：
   - 网络中断后恢复上传，从断点处继续
   - 上传相同文件时提示"文件已存在，是否覆盖？"
   - 支持暂停和取消上传

4. 文件分类：
   - 上传时可选择分类：文档、图片、视频、音频、压缩包、其他
   - 未选择分类时默认为"其他"
   - 支持拖拽文件到上传区域

5. 文件预览：
   - 图片文件支持缩略图预览
   - 文本文件支持在线预览前 5000 字符
   - 上传成功后自动刷新文件列表`
}
